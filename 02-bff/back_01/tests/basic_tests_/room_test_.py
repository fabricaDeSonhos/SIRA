from src.config import *
from common_test_ import *
    
'''    
# Example usage:
def basic_test_():

    # Create a new room
    room1 = create_object(Room, name="Conference Room")
    room2 = create_object(Room, name="Meeting Room")

    # Retrieve and print all rooms
    all_rooms = get_all_objects(Room)
    print(all_rooms)

    # Retrieve a specific room by ID
    print(get_object_by_id(Room, room1.id))

    # delete the room
    print("removing rooms (expecting 2 'Trues')")
    print(delete_object(room1))
    print(delete_object(room2))

    # check if there are no rooms
    print("NO MORE ROOMS: ")
    all_rooms = get_all_objects(Room)
    print(all_rooms)
'''